import React, {useEffect,useMemo,useRef,useState} from 'react'
import {createRoot} from 'react-dom/client'
import {HeartPulse, Utensils, GraduationCap, Mic, MessageCircle, ClipboardCheck, MapPin, Plus, Send, Volume2, ShieldAlert, ChevronRight, X} from 'lucide-react'
import './styles.css'
const API='/api', spots=[['Clover Food Lab','Kendall Square','soft grain bowls, soups'],['Caffè Nero','Central Square','tea, oatmeal, easy pickup'],['Life Alive','Central Square','warm bowls, smoothies'],['Saloniki','Cambridge','rice bowls, pita, delivery']]
const diet={ 'Sore throat':{label:'Soft + soothing',items:['Warm soup','Oatmeal','Smoothies','Tea with honey']}, 'Stomach upset':{label:'Gentle + bland',items:['Bananas','Rice','Applesauce','Toast']}, 'Fatigue':{label:'Simple + steady',items:['Grain bowls','Fruit','Soup','Water']}, 'Fever':{label:'Hydration first',items:['Broth','Electrolytes','Fruit','Easy snacks']} }
function App(){const [user,setUser]=useState(null),[tab,setTab]=useState('home'),[users,setUsers]=useState([]),[toast,setToast]=useState(''),[config,setConfig]=useState({}); const loadConfig=()=>fetch(API+'/config').then(r=>r.json()).then(setConfig); useEffect(()=>{fetch(API+'/users').then(r=>r.json()).then(setUsers);loadConfig()},[]); const updateUsers=u=>{setUsers(u);setUser(x=>u.find(y=>y.id===x?.id)||x)}; if(!user)return <Login onLogin={setUser}/>; const nav=[['home','Home',HeartPulse],['food','Food',Utensils],['academics','Academics',GraduationCap],['voice','Voice',Mic],['tracking','Track',ClipboardCheck],['forum','Community',MessageCircle]]; return <><aside><div className="brand"><HeartPulse/> Freshman <i>Flu</i></div><p className="small">Student support, made human.</p><div className="person"><span>{user.name[0]}</span><div><b>{user.name}</b><small>{user.credits} mock credits</small></div></div><nav>{nav.map(([id,l,I])=><button key={id} onClick={()=>setTab(id)} className={tab===id?'active':''}><I/> {l}</button>)}</nav><button className="switch" onClick={()=>setUser(null)}>Switch demo account</button></aside><main><header><div><span className="eyebrow">MIT · Boston + Cambridge</span><h1>{tab==='home'?'Hey, '+user.name+'.':tab[0].toUpperCase()+tab.slice(1)}</h1></div><div className="safe"><ShieldAlert/> Not medical advice. For urgent symptoms, contact <b>MIT Medical</b> or 911.</div></header>{tab==='home'&&<Home user={user} go={setTab}/>} {tab==='food'&&<Food user={user} users={users} updateUsers={updateUsers} toast={setToast}/>} {tab==='academics'&&<Academics user={user}/>} {tab==='voice'&&<Voice config={config} reloadConfig={loadConfig}/>} {tab==='tracking'&&<Tracking user={user}/>} {tab==='forum'&&<Forum user={user}/>}</main>{toast&&<div className="toast">{toast}<button onClick={()=>setToast('')}><X/></button></div>}</>}
function Login({onLogin}){const [name,setName]=useState('Student'),[password,setPassword]=useState('demo'),[err,setErr]=useState(''); const go=async e=>{e.preventDefault();let r=await fetch(API+'/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,password})});r.ok?onLogin(await r.json()):setErr((await r.json()).error)};return <div className="login"><div className="loginCard"><div className="logo"><HeartPulse/> Freshman Flu</div><h1>Support for the days you can’t push through.</h1><p>Demo account system · locally stored</p><form onSubmit={go}><label>Name</label><input value={name} onChange={e=>setName(e.target.value)}/><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{err&&<p className="error">{err}</p>}<button className="primary">Enter your space <ChevronRight/></button></form><small>Try <b>Student / demo</b> or <b>Volunteer / demo</b></small></div></div>}
function Home({user,go}){return <><section className="hero"><div><span className="pill">YOUR WELLBEING, FIRST</span><h2>What would make today feel a little lighter?</h2><p>We’ll help you communicate, prioritize, and find support—without asking you to do more than you can.</p></div><HeartPulse className="heroIcon"/></section><div className="cards">{[['food','Food support','Find gentle options or ask a peer for a pickup.',Utensils],['academics','Academic navigator','Turn your syllabus into a low-energy action plan.',GraduationCap],['voice','Find your voice','Practice or speak what you need to say.',Mic]] .map(([id,t,d,I])=><button className="card" onClick={()=>go(id)} key={id}><I/><h3>{t}</h3><p>{d}</p><span>Open <ChevronRight/></span></button>)}</div><section className="notice"><b>Small reminder:</b> Your health deserves care. This tool offers general support, not medical diagnosis or treatment. MIT students can contact MIT Medical for professional guidance.</section></>}
function Food({user,users,updateUsers,toast}){const [sym,setSym]=useState('Sore throat'),[requests,setRequests]=useState([]),[show,setShow]=useState(false); const load=()=>fetch(API+'/requests').then(r=>r.json()).then(setRequests);useEffect(load,[]);const rec=diet[sym];const claim=async id=>{let r=await fetch(API+'/requests/'+id+'/claim',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({userId:user.id})}),d=await r.json();if(r.ok){updateUsers(d.users);load();toast('Claimed! Mock credits transferred to your account.')}else toast(d.error)};return <div className="stack"><section className="feature"><div><span className="eyebrow">GENERAL FOOD IDEAS</span><h2>Eat for how you feel.</h2><p>Simple, non-medical ideas to make choosing food easier.</p><div className="chips">{Object.keys(diet).map(x=><button className={sym===x?'selected':''} onClick={()=>setSym(x)}>{x}</button>)}</div><h3>{rec.label}</h3><div className="tags">{rec.items.map(x=><span>{x}</span>)}</div></div><div className="foodArt">🥣</div></section><h2>Nearby, easy options</h2><div className="spotGrid">{spots.map(s=><article className="spot"><MapPin/><div><b>{s[0]}</b><small>{s[1]}</small><p>{s[2]}</p></div><button>View</button></article>)}</div><div className="splitTitle"><div><span className="eyebrow">PEER SUPPORT</span><h2>Food run board</h2></div><button className="primary" onClick={()=>setShow(true)}><Plus/> Ask for a pickup</button></div><div className="requestList">{requests.length===0&&<p className="empty">No requests yet. Ask for a low-energy food run.</p>}{requests.map(r=><article className="request"><div className="avatar">{r.requester[0]}</div><div><b>{r.title}</b><p>{r.requester} · {r.spot} · {r.cost} credits</p>{r.note&&<small>“{r.note}”</small>}</div>{r.status==='open'?r.requester_id===user.id?<span className="pill">Awaiting helper</span>:<button className="primary" onClick={()=>claim(r.id)}>Claim run</button>:<span className="claimed">✓ Claimed by {r.volunteer}</span>}</article>)}</div>{show&&<RequestModal user={user} close={()=>{setShow(false);load()}}/>}</div>}
function RequestModal({user,close}){const [f,setF]=useState({title:'Soup + tea pickup',spot:'Clover Food Lab, Kendall Square',cost:15,note:'I’m low energy—contactless handoff would help.'});const submit=async e=>{e.preventDefault();await fetch(API+'/requests',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...f,userId:user.id})});close()};return <div className="modalBg"><form className="modal" onSubmit={submit}><button type="button" className="close" onClick={close}><X/></button><h2>Ask for a food run</h2><label>What do you need?</label><input value={f.title} onChange={e=>setF({...f,title:e.target.value})}/><label>Spot</label><input value={f.spot} onChange={e=>setF({...f,spot:e.target.value})}/><label>Mock credit offer</label><input type="number" value={f.cost} onChange={e=>setF({...f,cost:e.target.value})}/><label>Helpful note</label><textarea value={f.note} onChange={e=>setF({...f,note:e.target.value})}/><button className="primary">Post request</button></form></div>}
function Academics({user}){const [course,setCourse]=useState('24.900 Introduction to Linguistics'),[text,setText]=useState('Attendance is expected. Students should notify the instructor promptly when illness affects participation. Late work is generally not accepted without prior communication. Make-up work may be considered for documented absences.'),[result,setResult]=useState(null),[loading,setLoading]=useState(false);const analyze=async()=>{setLoading(true);let r=await fetch(API+'/syllabus/text',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({userId:user.id,course,text})});setResult(await r.json());setLoading(false)};const file=async e=>{let f=e.target.files[0];if(!f)return;setLoading(true);let r=await fetch(API+'/syllabus',{method:'POST',headers:{'Content-Type':'application/pdf'},body:f});let d=await r.json();if(r.ok){setText(d.text);setResult(d)}else alert(d.error);setLoading(false)};return <div className="stack"><section className="feature academic"><div><span className="eyebrow">SYLLABUS → ACTION PLAN</span><h2>Know what can wait.</h2><p>Upload a syllabus or paste its text. We’ll surface attendance, late-work, and makeup language for you to review.</p></div><GraduationCap className="heroIcon"/></section><section className="panel"><div className="upload"><b>Upload syllabus PDF</b><p>Text extraction runs locally in this app.</p><label className="uploadBtn">Choose PDF<input type="file" accept="application/pdf" onChange={file}/></label></div><div className="or">or</div><div className="formgrow"><label>Course name</label><input value={course} onChange={e=>setCourse(e.target.value)}/><label>Paste syllabus text (always available)</label><textarea rows="7" value={text} onChange={e=>setText(e.target.value)}/><button className="primary" onClick={analyze} disabled={loading}>{loading?'Reading policy…':'Make my plan'} <ChevronRight/></button></div></section>{result&&<AcademicResult course={course} data={result.analysis}/>}</div>}
function AcademicResult({course,data}){const color=data.risk==='High'?'red':data.risk==='Low'?'green':'amber';const email=`Subject: Absence from ${course}\n\nHello Professor,\n\nI’m writing because I’m currently ill and may be unable to attend class / complete work as planned. I’m reviewing the course policy and wanted to ask what I should prioritize first and whether there is a make-up path for the affected work.\n\nThank you for your understanding,\n[Your name]`;return <section className="results"><div className="resultTop"><div><span className="eyebrow">{course}</span><h2>Your sick-day plan</h2></div><span className={'risk '+color}>{data.risk} policy risk</span></div><div className="policyGrid"><article><b>Attendance</b><p>{data.attendance}</p></article><article><b>Late & makeup work</b><p>{data.late}</p></article><article><b>What to do today</b><p>{data.action}</p></article></div><h3>Prioritize limited energy</h3>{data.assignments.map(a=><div className="assignment"><div><b>{a.name}</b><small>{a.why}</small></div><span className={'risk '+(a.priority==='High stakes'?'red':'amber')}>{a.priority}</span></div>)}<div className="email"><div><span className="eyebrow">READY-TO-SEND</span><h3>Absence email draft</h3></div><pre>{email}</pre><button onClick={()=>navigator.clipboard.writeText(email)}>Copy email</button></div></section>}
const twilioSetup=[['twilioAccount','TWILIO_ACCOUNT_SID and TWILIO_AUTH_TOKEN from the Twilio Console'],['twilioFromNumber','TWILIO_PHONE_NUMBER, your Twilio number in E.164 format, e.g. +16175551212'],['twilioPublicUrl','TWILIO_PUBLIC_URL, the public HTTPS tunnel base URL, e.g. https://your-name.ngrok-free.app']]
function Voice({config,reloadConfig}){const [text,setText]=useState('I need to reschedule my checkup and explain I have a fever and sore throat.'),[playing,setPlaying]=useState(false),[phone,setPhone]=useState(''),[calling,setCalling]=useState(false),[callNote,setCallNote]=useState(''),[relay,setRelay]=useState('I have a fever and sore throat. My voice is weak and I need help asking to reschedule my checkup.'),[audience,setAudience]=useState('doctor office'),[goal,setGoal]=useState('reschedule an appointment'),[tone,setTone]=useState('calm and direct'),[relayResult,setRelayResult]=useState(null),[relayNote,setRelayNote]=useState(''),[recording,setRecording]=useState(false);const recorder=useRef(null),chunks=useRef([]);const missing=twilioSetup.filter(([key])=>!config[key]),liveAgent=Boolean(config.twilioVoiceAgent);const speak=v=>{if(!('speechSynthesis'in window))return;setPlaying(true);let u=new SpeechSynthesisUtterance(v||text);u.onend=()=>setPlaying(false);speechSynthesis.speak(u)};const normalizePhone=v=>v.trim().replace(/[().\-\s]/g,'');const call=async()=>{const to=normalizePhone(phone);if(!window.confirm(liveAgent?`Place a real Twilio call to ${to||phone}? A live voice agent will hold the conversation, using the message below as context rather than reading it word for word.`:`Place a real Twilio call to ${to||phone}? It will speak the message shown below.`))return;setCalling(true);setCallNote('');try{let r=await fetch(API+'/call',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({to,text})}),d=await r.json();setCallNote(r.ok?'Call started. Check your phone and Twilio Console.':d.error)}catch{setCallNote('Could not reach the local call service.')}setCalling(false)};const compose=async(raw=relay)=>{setRelayNote('Drafting a speakable version…');setRelayResult(null);try{let r=await fetch(API+'/lost-voice/compose',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({raw,audience,goal,tone})}),d=await r.json();if(!r.ok)throw new Error(d.error);setRelayResult(d);setText(d.say);setRelayNote('Ready to play aloud or use for the call feature.')}catch(e){setRelayNote(e.message||'Could not draft the relay message.')}};const startRec=async()=>{setRelayNote('');try{let stream=await navigator.mediaDevices.getUserMedia({audio:true});chunks.current=[];recorder.current=new MediaRecorder(stream,{mimeType:MediaRecorder.isTypeSupported('audio/webm')?'audio/webm':undefined});recorder.current.ondataavailable=e=>e.data.size&&chunks.current.push(e.data);recorder.current.onstop=async()=>{stream.getTracks().forEach(t=>t.stop());setRecording(false);setRelayNote('Transcribing with Deepgram…');try{let blob=new Blob(chunks.current,{type:'audio/webm'}),r=await fetch(API+'/lost-voice/transcribe',{method:'POST',headers:{'Content-Type':blob.type},body:blob}),d=await r.json();if(!r.ok)throw new Error(d.error);setRelay(d.transcript||'');setRelayNote(d.transcript?'Deepgram transcript added.':'Deepgram did not hear enough audio.');if(d.transcript)compose(d.transcript)}catch(e){setRelayNote(e.message||'Transcription failed.')}};recorder.current.start();setRecording(true)}catch{setRelayNote('Microphone permission is needed to record a voice note.')}};const stopRec=()=>recorder.current?.state==='recording'&&recorder.current.stop();return <div className="stack"><section className="feature voice"><div><span className="eyebrow">IN-APP VOICE SUPPORT</span><h2>Say what you need to say.</h2><p>Practice a difficult call, turn weak speech into clear phrasing, or play a message aloud for someone nearby.</p></div><Mic className="heroIcon"/></section><section className="relayPanel"><div><span className="eyebrow">DEEPGRAM LOST VOICE RELAY</span><h2>Turn a whisper or rough note into something speakable.</h2><p>Record a short weak-voice note or type what you need. Deepgram transcribes the audio, then the relay drafts a clear message you can play aloud.</p></div><div className="relayGrid"><div className="relayInputs"><label>Who is this for?</label><input value={audience} onChange={e=>setAudience(e.target.value)}/><label>What do you need?</label><input value={goal} onChange={e=>setGoal(e.target.value)}/><label>Tone</label><input value={tone} onChange={e=>setTone(e.target.value)}/><label>Type or transcribe your rough message</label><textarea rows="6" value={relay} onChange={e=>setRelay(e.target.value)}/><div className="relayActions"><button className="primary" onClick={recording?stopRec:startRec}><Mic/> {recording?'Stop recording':'Record weak voice'}</button><button className="primary" onClick={()=>compose()}><Send/> Draft relay</button></div>{relayNote&&<small>{relayNote}</small>}</div><div className="relayOutput"><span className="pill">{config.deepgram?'DEEPGRAM TRANSCRIPTION ON':'ADD DEEPGRAM KEY'}</span><h3>Speakable message</h3>{relayResult?<><p className="sayBox">{relayResult.say}</p><button className="primary" onClick={()=>speak(relayResult.say)}><Volume2/> Speak this aloud</button><h4>If they push back</h4><p>{relayResult.backup}</p><div className="tags">{relayResult.keyPoints?.map(x=><span>{x}</span>)}</div></>:<p className="empty">Your drafted relay message will appear here.</p>}</div></div></section><section className="voicePanel"><div className="wave">◌ ◌ ◌ ◌ ◌</div><span className="pill">BROWSER SPEAKER</span><h2>Quick speaker mode</h2><p>Type any sentence and have your laptop say it aloud.</p><textarea rows="5" value={text} onChange={e=>setText(e.target.value)}/><button className="primary big" onClick={()=>speak(text)}><Volume2/> {playing?'Speaking…':'Speak this aloud'}</button><small>General communication support only—not medical advice.</small></section><section className="panel callPanel"><div><span className="eyebrow">OPTIONAL: REAL OUTBOUND CALL</span><h3>Experimental phone call bridge</h3><p>Twilio can attempt a real call, but trial accounts may block media streaming. The Deepgram relay above is the reliable demo path. {liveAgent?'This call will hand off to the live voice agent, which uses your message as context and talks with whoever answers.':'This call will read your message aloud.'}</p>{missing.length>0&&<div className="setupList"><b>Server setup still needed before a call can be placed:</b><ul>{missing.map(([key,label])=><li key={key}>{label}</li>)}</ul><small>Add these to <b>.env</b>, restart the server, then <button type="button" className="linkBtn" onClick={reloadConfig}>re-check setup</button>.</small></div>}</div><div className="callControls"><input aria-label="Phone number to call" placeholder="+16175551212" value={phone} onChange={e=>setPhone(e.target.value)}/><button className="primary" disabled={!phone||calling||missing.length>0} onClick={call}><Mic/> {calling?'Starting call…':'Place real call'}</button>{callNote&&<small className={callNote.startsWith('Call started')?'success':'error'}>{callNote}</small>}</div></section></div>}
function Tracking({user}){const [history,setHistory]=useState([]),[sym,setSym]=useState(['Fatigue']),[energy,setEnergy]=useState(3),[note,setNote]=useState('');const load=()=>fetch(API+'/checkins/'+user.id).then(r=>r.json()).then(setHistory);useEffect(load,[]);const save=async()=>{await fetch(API+'/checkins',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({userId:user.id,symptoms:sym,energy,note})});setNote('');load()}; const toggle=x=>setSym(s=>s.includes(x)?s.filter(y=>y!==x):[...s,x]);return <div className="stack"><section className="feature"><div><span className="eyebrow">DAILY CHECK-IN</span><h2>A tiny record, just for you.</h2><p>Track patterns and share them with a qualified professional if that would help. This is not a diagnostic tool.</p></div><ClipboardCheck className="heroIcon"/></section><section className="panel checkin"><div><h3>How are you feeling today?</h3><div className="chips">{Object.keys(diet).map(x=><button className={sym.includes(x)?'selected':''} onClick={()=>toggle(x)}>{x}</button>)}</div><label>Energy today: <b>{energy}/5</b></label><input type="range" min="1" max="5" value={energy} onChange={e=>setEnergy(+e.target.value)}/><label>Optional note</label><textarea value={note} onChange={e=>setNote(e.target.value)} placeholder="Anything you want to remember?"/><button className="primary" onClick={save}>Save today’s check-in</button></div><div className="trend"><span className="eyebrow">YOUR HISTORY</span><h3>Recent check-ins</h3>{history.length===0?<p className="empty">Your history will show up here.</p>:history.map(h=><div className="history"><div><b>{h.date}</b><small>{h.symptoms}</small></div><span>{'●'.repeat(h.energy)}{'○'.repeat(5-h.energy)}</span></div>)}</div></section></div>}
function Forum({user}){const [posts,setPosts]=useState([]),[query,setQuery]=useState(''),[form,setForm]=useState(false),[f,setF]=useState({title:'',body:'',symptoms:''});const load=()=>fetch(API+'/posts').then(r=>r.json()).then(setPosts);useEffect(load,[]);const submit=async e=>{e.preventDefault();await fetch(API+'/posts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...f,userId:user.id})});setForm(false);setF({title:'',body:'',symptoms:''});load()};let shown=posts.filter(p=>(p.title+p.body+p.symptoms).toLowerCase().includes(query.toLowerCase()));return <div className="stack"><section className="feature"><div><span className="eyebrow">STUDENT COMMUNITY</span><h2>You’re not the only one.</h2><p>Share practical support and low-stakes advice. Keep it kind, general, and never diagnostic.</p></div><MessageCircle className="heroIcon"/></section><div className="forumBar"><input placeholder="Search by symptom or topic…" value={query} onChange={e=>setQuery(e.target.value)}/><button className="primary" onClick={()=>setForm(true)}><Plus/> New post</button></div><div className="postList">{shown.map(p=><Post key={p.id} post={p} user={user}/>)}</div>{form&&<div className="modalBg"><form className="modal" onSubmit={submit}><button type="button" className="close" onClick={()=>setForm(false)}><X/></button><h2>Share with the community</h2><label>Question or topic</label><input required value={f.title} onChange={e=>setF({...f,title:e.target.value})}/><label>What would help to know?</label><textarea required value={f.body} onChange={e=>setF({...f,body:e.target.value})}/><label>Related symptoms (optional)</label><input value={f.symptoms} onChange={e=>setF({...f,symptoms:e.target.value})}/><button className="primary">Post safely</button></form></div>}</div>}
function Post({post,user}){const [replies,setReplies]=useState([]),[open,setOpen]=useState(false),[reply,setReply]=useState('');const [count,setCount]=useState(post.replies);useEffect(()=>setCount(post.replies),[post.replies]);const load=()=>fetch(API+'/posts/'+post.id+'/replies').then(r=>r.json()).then(list=>{setReplies(list);setCount(list.length)});const toggle=()=>{setOpen(!open);if(!open)load()};const send=async e=>{e.preventDefault();await fetch(API+'/posts/'+post.id+'/replies',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({userId:user.id,body:reply})});setReply('');load()};return <article className="post"><div className="avatar">{post.name[0]}</div><div className="postBody"><small>{post.name} · community</small><h3>{post.title}</h3><p>{post.body}</p>{post.symptoms&&<div className="tags"><span>{post.symptoms}</span></div>}<button className="replyBtn" onClick={toggle}><MessageCircle/> {count} replies</button>{open&&<div className="replies">{replies.map(r=><p><b>{r.name}:</b> {r.body}</p>)}<form onSubmit={send}><input required value={reply} onChange={e=>setReply(e.target.value)} placeholder="Offer a general, supportive reply…"/><button><Send/></button></form></div>}</div></article>}
createRoot(document.getElementById('root')).render(<App/>)
